services:
  smart-pig-farm:
    build: .
    container_name: smart-pig-farm-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: always

volumes:
  data:

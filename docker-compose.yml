services:
  smart-pig-farm:
    build: .
    container_name: smart-pig-farm-app
    ports:
      - "7731:7731"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: always

volumes:
  data:
